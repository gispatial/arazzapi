import PageTemplate from "../templates/Template";
import Screening_PackagesTable from "./Screening_PackagesTable";
import React from "react";

const Screening_PackagesPage = (props) => {
    return(
    <PageTemplate title="Screening_Packages List">
        <Screening_PackagesTable/>
    </PageTemplate>)
}
export default Screening_PackagesPage;
import PageTemplate from "../templates/Template";
import Screening_PackagesTable from "./Screening_PackagesTable";
import React from "react";

const Screening_PackagesPage = (props) => {
    return(
    <PageTemplate title="Screening_Packages List">
        <Screening_PackagesTable/>
    </PageTemplate>)
}
export default Screening_PackagesPage;
